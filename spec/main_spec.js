let Block = require('../main.js');
console.log(Block);

describe('block creation', function() {
  beforeEach(function() {
    testBlock = new Block.Block();
  });
  it('returns the correct data assigned to the block', function() {

  });
});
